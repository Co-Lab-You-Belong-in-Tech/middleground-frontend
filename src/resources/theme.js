import { createMuiTheme } from "@material-ui/core/styles";
const theme = createMuiTheme({
  palette: {
    primary: {
      main: "#B488D9",
    },
    action: {
      hover: "#B488D9",
      hoverOpacity: 1,
      selected: "#D2F42F",
    },
  },
});

export default theme;
